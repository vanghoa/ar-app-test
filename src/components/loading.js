import React from 'react';

export default function Loading() {
    return (
        <section className="absolute z-50 left-0 top-0 w-full h-full bg-yellow-500 flex justify-center items-center font-mono text-lg p-8">
            Any moment now... Hold on!
        </section>
    );
}
